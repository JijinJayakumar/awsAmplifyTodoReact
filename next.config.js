/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["img.icons8.com", "picsum.photos", "images.unsplash.com"],
  },
};

module.exports = nextConfig
